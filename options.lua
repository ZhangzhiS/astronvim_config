-- 在此处设置vim选项 (vim.<first_key>.<second_key> =  value)
return {
  opt = {
    relativenumber = true, -- 设置相对行号
    tabstop = 4, -- tab 宽度为4
    autoindent = true,
  },
  g = {
    mapleader = " ", -- 设置leader键
  },
}
